---
name: setup-debug
description: Create or update a repository Debugfile.yml by discovering architecture, developer documentation, CI/CD, services, tests, observability channels, provider CLIs, access boundaries, recovery signals, and RCA destinations. Use when the user asks to set up, configure, or refresh project debugging channels.
---

# Setup debug

Call the Skill tool with `debug` and run its Setup mode. The two skills are distributed and installed together; `debug` owns the Debugfile schema, template, validator, and setup procedure.
