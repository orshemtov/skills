# Python review

Apply this reference when the pull request contains changed `.py` files.

Use the Python version, dependency configuration, conventions, and test commands declared by the repository. Treat style preferences as findings only when they obscure behavior or violate an explicit project rule.

Detailed Python review guidance will be added from the reviewed source document.
